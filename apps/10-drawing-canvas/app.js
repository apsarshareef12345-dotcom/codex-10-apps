const key='canvas.image',ctx=canvas.getContext('2d');let drawing=false,last,history=[],future=[];function snap(){history.push(canvas.toDataURL());if(history.length>30)history.shift();future=[];localStorage.setItem(key,canvas.toDataURL())}function restore(url){let im=new Image();im.onload=()=>{ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(im,0,0);localStorage.setItem(key,url)};im.src=url}function pos(e){let r=canvas.getBoundingClientRect(),p=e.touches?e.touches[0]:e;return{x:(p.clientX-r.left)*canvas.width/r.width,y:(p.clientY-r.top)*canvas.height/r.height}}function down(e){e.preventDefault();snap();drawing=true;last=pos(e)}function draw(e){if(!drawing)return;e.preventDefault();let p=pos(e);ctx.strokeStyle=color.value;ctx.lineWidth=size.value;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(last.x,last.y);ctx.lineTo(p.x,p.y);ctx.stroke();last=p}function up(){if(drawing){drawing=false;localStorage.setItem(key,canvas.toDataURL())}}canvas.onpointerdown=down;canvas.onpointermove=draw;window.onpointerup=up;undo.onclick=()=>{if(history.length){future.push(canvas.toDataURL());restore(history.pop())}};redo.onclick=()=>{if(future.length){history.push(canvas.toDataURL());restore(future.pop())}};clear.onclick=()=>{snap();ctx.clearRect(0,0,canvas.width,canvas.height);localStorage.removeItem(key)};download.onclick=()=>{let a=document.createElement('a');a.href=canvas.toDataURL('image/png');a.download='drawing.png';a.click()};let saved=localStorage.getItem(key);if(saved)restore(saved);else{ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height)}
