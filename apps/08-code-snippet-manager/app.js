const key='snippets.data';let items=JSON.parse(localStorage.getItem(key)||'[]');function save(){localStorage.setItem(key,JSON.stringify(items))}function esc(s){return s.replace(/&/g,'&amp;').replace(/</g,'&lt;')}function render(){list.innerHTML=items.map(x=>'<article class="card snippet"><header><div><b>'+esc(x.title)+'</b> <span class="pill">'+x.lang+'</span></div><div><button class="ghost copy" data-id="'+x.id+'">Copy</button> <button class="danger del" data-id="'+x.id+'">×</button></div></header><pre><code>'+esc(x.code)+'</code></pre></article>').join('')||'<p class="empty">Save your first snippet.</p>';document.querySelectorAll('.copy').forEach(b=>b.onclick=async()=>{await navigator.clipboard.writeText(items.find(x=>x.id==b.dataset.id).code);b.textContent='Copied!';setTimeout(()=>b.textContent='Copy',900)});document.querySelectorAll('.del').forEach(b=>b.onclick=()=>{items=items.filter(x=>x.id!=b.dataset.id);save();render()})}form.onsubmit=e=>{e.preventDefault();items.unshift({id:Date.now(),title:title.value,lang:lang.value,code:code.value});save();form.reset();render()};render();
